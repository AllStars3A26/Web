{% extends 'basegeneral.html.twig' %}
{% block body %}
  <!-- Content Wrapper. Contains page content -->
  <div class="content-wrapper">
    
    <!-- Main content -->
    <section class="content">
      <div class="container-fluid">
        <div class="row">
          <!-- left column -->
          <div class="col-md-12">
            <!-- general form elements -->
            <div class="card card-primary">
              <div class="card-header">
                <h3 class="card-title">Ajouter un terrain</h3>
              </div>
              <!-- /.card-header -->
              <!-- form start -->

                {{ form_start(formTerrain,{'attr': {'novalidate': 'novalidate'}} ) }}

                <div class="card-body">
                  
                 
                  <div class="form-group">
                    <label for="exampleInputPassword1">Nom du terrain </label>
                    {{ form_widget(formTerrain.NomTerrain) }}
                    {{ form_errors(formTerrain.NomTerrain, {'attr': {"class":"mb-0"}}) }}
                  </div>
                  <div class="form-group">
                    <label for="exampleInputPassword1">Type du terrain </label>
                    {{ form_widget(formTerrain.TypeTerrain) }}
                    {{ form_errors(formTerrain.TypeTerrain, {'attr': {"class":"mb-0"}}) }}
                  </div>
                  <div class="form-group">
                    <label for="exampleInputPassword1">Description du terrain </label>
                    {{ form_widget(formTerrain.DescriptionTerrain, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(formTerrain.DescriptionTerrain, {'attr': {"class":"mb-0"}}) }}
                  </div>
                   <div class="form-group">
                    <label for="exampleInputPassword1">Adresse du terrain </label>
                    {{ form_widget(formTerrain.AdresseTerrain, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(formTerrain.AdresseTerrain, {'attr': {"class":"mb-0"}}) }}
                  </div>
                  <div class="form-group">
                    <label for="exampleInputPassword1">Disponibilite (1:oui , 0:non)</label>
                    {{ form_widget(formTerrain.Disponibilite, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(formTerrain.Disponibilite, {'attr': {"class":"mb-0"}}) }}
                  </div>
               
                </div>
                <!-- /.card-body -->

                <div class="card-footer">
                 {{ form_widget(formTerrain.Ajouter, {'attr': {'class': 'btn btn-primary'}}) }}
                </div>
             {{ form_end(formTerrain) }}
            </div>
            <!-- /.card -->

          

          </div>

        </div>
        <!-- /.row -->
      </div><!-- /.container-fluid -->
    </section>
    <!-- /.content -->
  </div>
  <!-- /.content-wrapper -->
  
{% endblock %}


