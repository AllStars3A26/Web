{% extends 'baseback.html.twig' %}
{% block body %}
  <!-- Content Wrapper. Contains page content -->
  <div class="content-wrapper">
    
    <!-- Main content -->
    <section class="content">
      <div class="container-fluid">
        <div class="row">
          <!-- left column -->
          <div class="col-md-12">
            <!-- general form elements -->
            <div class="card card-primary">
              <div class="card-header">
                <h3 class="card-title"><b>Modifier une séance<b></h3>
              </div>
              <!-- /.card-header -->
              <!-- form start -->

                {{ form_start(formSeanceM,{'attr': {'novalidate': 'novalidate'}} ) }}

                <div class="card-body">
                  
                 
                  <div class="form-group">
                    <label for="exampleInputPassword1">Date de la séance </label>
                    {{ form_widget(formSeanceM.dateSeance) }}
                    {{ form_errors(formSeanceM.dateSeance, {'attr': {"class":"mb-0"}}) }}
                  </div>
                  <div class="form-group">
                    <label for="exampleInputPassword1">Heure de la séance </label>
                    {{ form_widget(formSeanceM.heureSeance) }}
                    {{ form_errors(formSeanceM.heureSeance, {'attr': {"class":"mb-0"}}) }}
                  </div>
                  <div class="form-group">
                    <label for="exampleInputPassword1">Nom de la séance </label>
                    {{ form_widget(formSeanceM.nomT, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(formSeanceM.nomT, {'attr': {"class":"mb-0"}}) }}
                  </div>
                   <div class="form-group">
                    <label for="exampleInputPassword1">Nom de l'entraineur </label>
                    {{ form_widget(formSeanceM.nomE, {'attr': {'class': 'form-control'}}) }}
                    {{ form_errors(formSeanceM.nomE, {'attr': {"class":"mb-0"}}) }}
                  </div>
                  
               
                </div>
                <!-- /.card-body -->

                <div class="card-footer">
                 {{ form_widget(formSeanceM.Modifier, {'attr': {'class': 'btn btn-primary'}}) }}
                </div>
             {{ form_end(formSeanceM) }}
            </div>
            <!-- /.card -->

          

          </div>

        </div>
        <!-- /.row -->
      </div><!-- /.container-fluid -->
    </section>
    <!-- /.content -->
  </div>
  <!-- /.content-wrapper -->
  
{% endblock %}


