{% extends 'front/client.html.twig' %}
{% block body %}

    <img src="{{ qrCode }}">
    <a href="{{ path('afficher_tournoif') }}">
        <button class="btn btn-sm btn-outline-primary">return</button>
    </a>
{% endblock %}